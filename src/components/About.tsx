import React from "react";
import TeamSectionDemo from "@/components/ui/team-section-demo";

export default function About() {
  // TeamSectionDemo already includes its own header/description — render it directly.
  return <TeamSectionDemo />;
}
